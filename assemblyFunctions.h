#include <iostream>
#include <stdio.h>
#include <string.h>
#include<conio.h>
#include <fstream>
using namespace std;




int movl(int reg1, int reg2){
  reg1 = reg2;
  return reg2;
}

int jmp(string linha){
  //Procurar a linha da label
}

int add(int x, int reg1){
  x = reg1;
  return x;
}

int inc(int x){
  x = x + 1;
  return x;
}

int cmp(int reg1, int reg2){
  reg1 = reg2;
  return reg1;
}

int jle(int reg1,int reg2){
  if(reg1 <= reg2){

  }
}
